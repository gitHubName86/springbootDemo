spring:
  datasource:
    name: springboot
    url: jdbc:mysql://localhost:3307/springboot
    username: root
    password: 680991
    driver-class-name: com.mysql.jdbc.Driver
  thymeleaf:
    cache: false
    encoding: utf-8
    servlet:
      content-type: text/html
    mode: LEGACYHTML5

mybatis:
  mapper-locations: classpath:mapper/*.xml  #配置映射文件
  type-aliases-package: com.fzp.bean #配置实体类

server:
  servlet:
    context-path: /springboot
